import { AppBar, Toolbar } from '@material-ui/core'
import React from 'react'

const SiteHeader = () => {
    return (
        <AppBar>
            <Toolbar>
                jkasdhk
            </Toolbar>
        </AppBar>
    )
}

export default SiteHeader
